# 2026-09-24 — v1.65.0 release：去識別化「AI 智慧偵測」＋「我的規則」

## 出貨前重驗（使用者一句「release」）
- 主 repo 36 檔修改＋21 新檔全部未 commit，先重跑驗證再拆 commit：redaction crate 366＋18＋7、gateway `redaction` 篩選 150、cli `redaction` 篩選 19、web vitest 244 檔／1908 全綠。
- 坑：web vitest 與 cargo test 同時跑會炸成「205 檔失敗、只跑 800 個」（worker 資源衝突，5 秒結束），單獨重跑即 1908 全綠——並行驗證時 vitest 要獨占跑。
- 文件補洞（memory 記的欠帳）：`feature-inventory.md` Data Sources 列、`docs/README.md` 55 列、`CLAUDE.md` 資料來源段各補上 AI 智慧偵測＋我的規則；三語 55-data-sources.md 早已有「Custom rules」與「AI detection」兩節。

## commit 拆分（沿用 1.64.0 的後端／前端／文件三段式）
- 78d3c95a `feat(redaction)`：NER 規則型別＋`ai_pii` profile＋`redaction.model.*`＋我的規則七支 RPC＋`suggest_pattern`＋草稿 dry_run＋`enabled`／`[meta.labels]`；inference `onnx` feature 修復＋`opf_spike` 範例。
- 36308646 `feat(web)`：AI 智慧偵測列三態、我的規則卡＋三步精靈、規則包匯入對話、api.ts、三語 i18n。
- 10947e0e `docs`：feature doc 三語、inventory／README／CLAUDE.md、CHANGELOG Unreleased、09-23 日報。
- 未入庫的雜檔刻意留著：`appdb_test.html`、`cat-ref.jpg/.zip`、`private_scratch/`（50 個 Yocto log／截圖，未在 .gitignore）、commercial 的 `y14-b-shot2*.ppm`。

## release.sh minor → 1.65.0
- 標題「去識別化 AI 智慧偵測×我的規則×CI Windows 修復」；bump commit 9f6b3b30，tag `v1.65.0`＋`desktop-v1.65.0` 已 push，GitHub 四條 workflow 同時起（Release／desktop-release／Docker image／CI）。
- 坑：macOS 沒有 `setsid`，第一次 `nohup setsid` 根本沒啟動；純 `nohup … &`＋`disown` 即可存活。
- Pro image 走 Cloud Build（build 758965cb）：09:23 送出、09:34 SUCCESS（約 11 分鐘，E2_HIGHCPU_32），AR `duduclaw-pro:v1.65.0`＋`latest`；release.sh 自己 pull＋docker save 上傳離線 tar 到 `gs://duduclaw-oem-images/duduclaw-pro/`。
- pro bin：darwin-x64 趁 Cloud Build 遠端跑時本機並行建（4m34s、132 MB）→ tar＋sha256＋minisign 三件套已上 `gs://duduclaw-oem-images/duduclaw-pro-bin/v1.65.0/`；darwin-arm64／linux-x64 由 release.sh 自建自傳（image 成功時 release.sh 本來就會 `docker create`＋`docker cp` 抽 linux-x64——1.64.0 要手補只因為 image 首發炸掉）；三平台 ×（tar＋sha256＋minisig）全在 bucket。control-plane `DUDUCLAW_PRO_VERSIONS=v1.65.0,v1.64.0,v1.63.0` 已由 release.sh 更新（新 revision）。release.sh 全程 exit=0，無 WARNING／skip。
- 坑：照 release.sh 的 `PRO_BIN_BUCKET=` 行 grep 出來是 `${DUDUCLAW_IMAGE_TAR_BUCKET:-duduclaw-oem-images}` 字面值，手打包時要自己展開。

## verify
- GitHub Actions：Release（五平台 build／release／pypi-publish／npm-publish）與 desktop-release（四平台＋updater-manifest）全 success；CI main 綠；GH Release `v1.65.0` 25 個資產（五平台 tar＋minisig／pem／sha256／sig）非 draft。
- `release.sh verify 1.65.0` 六項全 OK：PyPI／npm／pro-image／oem-tar／pro-bin-mac／pro-bin-linux。
- Docker image workflow（run 35942644145）仍在跑（歷次 3.5–5 小時，job 上限 6 小時；這版多了 ort／tokenizers 依賴，要留意是否撞上限）。
- 全程時間：09:10 開始重驗 → 09:20 release.sh 起跑 → 09:22 tag push → 09:44 RELEASE_DONE → 09:5x GH 兩條 workflow 完成。

## 收尾
- commercial：設計文件「落地: v1.65.0」＋INDEX §1 v1.65 列＋§2 狀態 ✅、pro-gateway Cargo.lock → 36e76db 已 push。
- 殘欠帳：瀏覽器 UI 走查（AI 智慧偵測列／我的規則精靈）、Windows／Linux 真機 ORT 動態載入、`private_scratch/` 是否進 .gitignore。
