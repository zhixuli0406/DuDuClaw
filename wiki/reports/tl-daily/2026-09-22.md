# 2026-09-22 — 去識別化「資料表欄位」規則（JsonPath 引擎＋Odoo db_field 語法糖）

## 起因
使用者問「去識別化能不能指定資料庫資料表欄位」。盤點結論：RFC-23 管線只有內容比對（regex／keyword）＋來源×PII 類別過濾，`RuleKind::JsonPath` 只佔 enum 位、引擎 warn 跳過；MCP 節流點只走字串葉節點看不到 key。拍板 C：通用 JsonPath 引擎為底，Odoo `model.field` 為語法糖。

## 落地（全部未 commit）
- 設計：`commercial/docs/DESIGN-redaction-field-rules-2026-09.md`（INDEX 安全段已加行）。
- 程式（Opus 實作）：`crates/duduclaw-redaction/src/rules/{json_path,db_field}.rs` 新檔；`rules/mod.rs`（`JsonPath` 加 `match_args`／`exclude_keys`、新 `DbField`）、`engine.rs`（結構化規則獨立編譯、`apply_structured`）、`pipeline.rs`（`ToolContext`／`redact_value` 三段 pass、`mint_token` 共用、token 不二次去識別化）、`audit.rs`（`Redact.path`）；`duduclaw-cli` 的 `mcp_redaction.rs`／`mcp_dispatch.rs` 把工具引數帶進管線；`redaction_verify.rs`＋`lib.rs` 新增 JSON 模式（`--tool`／`--arg k=v`）；`docs/examples/redaction-sample-odoo.json`。
- 文件（Sonnet）：operator guide 新節、RFC-23 實作表更正（`IdentityRule` 誠實標未實作）、CHANGELOG Unreleased、三語 feature-inventory。

## 驗收（Fable）
- 測試：redaction 179 unit＋14 e2e（基線 122＋11）、cli 11、`cargo check` cli／gateway、clippy 新檔零警告。
- 活體（`duduclaw redaction verify` JSON 模式，真 binary、暫存 DUDUCLAW_HOME）：一般欄位 15 命中／15 可還原；`res.partner.*` 15 命中全在 `/content/0/text#/…` 且 `id` 保留；`model=crm.lead` 欄位規則零命中僅 pattern 7 筆；CSV 舊模式 15/15 不變。
- **抓到並修掉一個 bug**：`model.*` 的 `$` 根路徑在外層 pass 會把 MCP 信封整段（含 `type`）token 化；修法 `structured_pass(allow_root)` 外層為 false，回歸測試 +3。
- 環境：Xcode 授權未接受導致全 workspace link 失敗，先以 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 頂住，使用者已 `sudo xcodebuild -license accept` 解除。實作 agent 為了讓 `duduclaw-cli` 編過補了 `crates/duduclaw-dashboard/dist/` 佔位檔，驗收端改跑 `web/ npm ci && npm run build` 產真 dist（5.2M）。

## 欠帳／待拍板
1. commit（使用者關卡）：建議主 repo 一個 `feat(redaction): field-level json_path/db_field rules` ＋ commercial 巢狀 repo 一個 `docs` commit。工作樹另有非本波的未追蹤檔（`appdb_test.html`、`cat-ref.*`、`private_scratch/`）勿誤 stage。
2. dashboard 欄位規則編輯器（`redaction.get/update` 目前不下發 `rules`）——新 UI，先流程確認再 /design 畫布。
3. dashboard「欄位範圍選擇器」實為 PII 類別，建議改名「資料類型」。
4. `IdentityRule`／`NerRule` 仍未實作（RFC-23 舊欠帳）。
5. `mcp_redaction` 在 pipeline 建構失敗時仍 passthrough（既有 fail-open，本波未動，建議下波收斂為 fail-closed）。
6. serde_json 未開 `preserve_order`，內嵌 JSON 重序列化 key 變字母序（已列已知限制）。
7. 未對真 Odoo 實例活測；ODOO_TOOLS 對照表依 mcp.rs／mapper struct 建立並有 `every_expanded_path_compiles` 測試護欄。

## 第二波：欠帳清理（使用者指令「將欠帳完成」）
- **IdentityRule 落地**（RFC-23 標 v1.14.0 ✅ 卻從未實作）：`rules/identity.rs` 讀 `<home>/shared/wiki/identity/people/*.md` 的 `display_name`，沿用 keyword 比對（抽成 `match_needles`），目錄 mtime／60 秒惰性刷新；`source` 只接受 `wiki`（可省略），目錄缺失即載入錯誤，零人 warn 不命中。identity crate 補 `list_people_sync`／`for_people_dir`。`EngineOptions`＋`ManagerPaths.identity_people_dir` 接線；engine 移除 warn-skip 萬用分支（match 現在窮舉）。
- **MCP 層 fail-closed 三處**：每次呼叫 pipeline 建構失敗 → 整個 result 換佔位；`mcp-server`／`http-server` 在 `enabled = true` 但初始化失敗時拒絕啟動；`[redaction]` 解析失敗（含 TOML 語法錯）一律 `Err`，verify 也不再退回 `general`。
- **`match_needles` 保長度折疊**：lowercase 後位元組長度不同或展開成多字元者維持原字，堵掉 `text[start..end]` 切在字元中間的 panic（實證 `İ台積電` 舊碼會 panic）；needle 也用同一折疊。
- **dashboard 用語**：`redaction.fields.*`／`redaction.profiles.desc` 三語「欄位」→「資料類型」，key 不變，元件只改註解。
- 驗收：redaction 196＋16、identity 39＋1、cli 18、web i18n 4 全綠；活測五案全過（人名 3/3 可還原、目錄缺失報錯、壞設定 verify 報錯、mcp-server 拒啟兩式、關閉對照組正常）。活測抓修三個真缺陷（identity source 非 default、`.ok()` 吞解析錯誤、折疊位移）。
- 未能完成：真 Odoo 活測（本機無 `[odoo]`）、serde_json `preserve_order`（不冒全 workspace 風險）、dashboard 編輯器（流程提案在設計文件 §11 待確認）。
- 待拍板：gateway 開機時 `[redaction]` 解析失敗只有 DEBUG log 且繼續無去識別化運作（`server.rs:425-433`），manager 建構失敗只 warn 也繼續（`server.rs:454`）——同類 fail-open，改法需拍板（拒絕開機 vs 開機但標記毒化拒絕工具結果）。

## 第三波：拍板 A（編輯器）＋ B（gateway 毒化）
- 設計畫布（Artifact aaa4fbe8…，備份 `commercial/design/redaction-field-rules-editor/canvas.html`）七畫面待視覺拍板；前端未動。
- 後端落地：`redaction.get` 加 `field_rules`／`poisoned`；`redaction.update` 收 `field_rules` upsert-merge（id 規則、只管 db_field/json_path、寫入前用共用 `resolve_rule_specs` 試編譯）；新 `redaction.dry_run`（不回原值）；共用 helper 搬到 `duduclaw_redaction::locate`。gateway 開機三態分類（`classify_redaction_boot`），壞設定進毒化：ERROR log＋Activity `redaction_init_failed`，熱重載成功清除＋`redaction_recovered`。
- 驗收：redaction 200＋16、cli 14、gateway 子集 39、tsc 綠；活測 gateway 真開機毒化路徑通過、對照組乾淨。抓到並補上：第三波 agent 沒重建主 binary（只跑 check），活測前先 `cargo build -p duduclaw-cli`。
- 未 commit；文件同步（operator guide／CHANGELOG）進行中。

## 第四輪：使用者指出「沒有資料來源概念、只能走 Odoo」
- 實證：外部 MCP server 回傳直接進 Claude CLI、openai-compat 工具迴圈亦無去識別化、無 SQL driver、無資料來源概念。
- 設計 §13：資料來源登錄（所有方案必做，Odoo 變內建登錄項）＋ 資料流三案 A（MCP proxy）／B（原生 SQL 連接器）／C（分期），建議 C。畫布加畫面 8「資料來源」＋畫面 3 加來源下拉。待拍板。

## 第五波：拍板 A＋B（資料來源登錄＋MCP proxy＋原生 SQL 連接器＋前端八畫面）
- 四包平行落地：WP-R `data_source.rs` 登錄（內建 `odoo`／`duduclaw_db`，`db_field.source`，`redaction.get/update data_sources`）；WP-P `duduclaw mcp-proxy`＋spawn 時 `.mcp.json` 改寫（`DUDUCLAW_MCP_PROXY_ENV` 傳 env）＋`duduclaw-llm::ToolInterceptor`（openai-compat／direct-API 工具迴圈）；WP-D 新 crate `duduclaw-db`（sqlx 0.8.6 pg/mysql/sqlite 唯讀三重保證、`[db_sources.*]`、`[capabilities] db_sources`、四工具、`db:read`、`db_sources.*` RPC）；WP-W 前端（欄位規則卡／資料來源卡／試跑／毒化橫幅，三語）。
- 驗收：全量 gateway 6547／cli 1377／core 617、各新 crate 全綠、web 1756、i18n 5313 對齊；活測 proxy loopback、SQLite e2e、PostgreSQL／MySQL Docker e2e 全過。活測抓到兩個真 bug（pg NUMERIC 小數位、MySQL 表清單解碼吞錯）已修。
- 裁決：`db_query` 只在 `allowed_tables = ["*"]` 開放；`db_sources.test` 單一回應形狀；線上 `connector` 承載已解析 source。
- 新依賴：sqlx 0.8.6（launchbadge，features 最小化）＋其傳遞依賴（rsa／whoami／dotenvy／md-5 等）。
- 文件覆核抓到 `claude_runner.rs` 派工路徑未接 proxy，已補（gateway 全量 6552 綠）。未涵蓋（誠實）：PTY session pool（預設關）、codex／gemini／antigravity 外部 MCP、HTTP/SSE MCP server、`local_llm.rs` 工具迴圈；真 Odoo 活測仍缺實例。
- 全部未 commit；文件包（feature doc 55 三語／operator guide／CHANGELOG／CLAUDE.md／ARCHITECTURE）進行中。

## 第六波：地端 CSV／Excel（使用者「做」）
- 盤點：內建 Read／Bash 不經節流點、附件只給路徑、office_script 僅樣態。設計 §14。
- 落地：`mcp_files.rs`（file_read／csv_read／xlsx_read，calamine =0.36.1，路徑圍欄＋容量上限，scope `files:read` 24）；登錄層 `TableSource::FromResult`／`match_result`／`free_form_names`／`['key']` Unicode；內建來源 `duduclaw_files`；`data-file-guard.sh` PreToolUse hook（`DUDUCLAW_DATA_FILE_GUARD` on/read_only/off，預設 on，隨安全 hook 安裝）；附件提示改指向新工具；前端自由命名驗證、「看工具結果」模式、守門下拉、三語。
- 驗收：全量與子集全綠；活測 CSV／xlsx（中文表頭）整值遮蔽、圍欄、hook 三態全過。
- 限制：hook 為 shell（Windows 無 bash 失效）、Bash 檔名啟發式；`db_query`／`office_script` 走樣態。

## 本機替換活測前置：Pro wrapper 子命令分派缺陷（正式環境既有 bug）
- 現況：launchd `com.duduclaw.gateway` 跑 `/usr/local/bin/duduclaw-pro`（x86_64、8/17 建），agent `.mcp.json` 全指向 `duduclaw-pro mcp-server`，但 wrapper main 完全不看 argv、一律開 gateway（實測：`duduclaw-pro mcp-server` 在暫存 home 生出 approvals.db 並常駐）。本機從無任何 `tool_calls.jsonl`，與「MCP 工具面從未接通」一致。
- 修法：`commercial/duduclaw-pro-gateway` 加 `duduclaw-cli` 依賴，main 在 tracing 初始化前檢查 argv：無參數／`gateway`／`run` 走 Pro gateway，其餘子命令（`mcp-server`、`mcp-proxy`、`--version`…）委派 `duduclaw_cli::entry_point()`。
- 替換流程照記憶 SOP：pro `cargo build --release --bin duduclaw-pro` → 備份換 `/usr/local/bin/duduclaw-pro` → `launchctl kickstart -k gui/501/com.duduclaw.gateway`；CE 走 `scripts/dev-replace-binary.sh --skip-web`。

## 本機替換完成（23:07–23:10）
- CE：`scripts/dev-replace-binary.sh --skip-web` → npm 位置 binary 已換（備份 `duduclaw.bak-20260922-230615`），版本 1.63.0（workspace 版）。
- Pro：`target/release/duduclaw-pro`（arm64，含子命令分派）→ `/usr/local/bin/duduclaw-pro`（備份 `.bak-20260922-230653`）→ `launchctl kickstart -k gui/501/com.duduclaw.gateway`；dashboard 200、Discord 重連。
- **第二個既有正式環境斷鏈**：內部 MCP key 建於 08-16、09-15 起過期（mcp_auth 上限 30 天），`ensure_internal_mcp_key` 不看年齡沿用 → 本機 agent 一週無工具（`duduclaw doctor` 實錘）。手動輪換：備份 `config.toml.bak-20260922-230958`、移除過期內部 key、重啟 → 新 key 配發並自動寫回所有 agent `.mcp.json`；以 agnes 的 `.mcp.json` 原樣 spawn 驗證 `tools/list` 221 個工具（含 csv_read/xlsx_read）。永久修法（開機自動輪換 25 天）派 Opus 實作中，落地後再重建替換一次。
- 23:29–23:37 永久修法落地：`ensure_internal_mcp_key` 開機自動輪換（25 天輪換、>30 天與無法解析者清除、25–30 天保留給在途子行程、穩態零寫入）、doctor 文案更新、mcp_template 補覆寫回歸測試（原本就會覆寫）；CHANGELOG Fixed。重建 pro＋CE 再替換（備份 `duduclaw-pro.bak-20260922-233728`、`duduclaw.bak-20260922-233724`），gateway 第三次重啟（pid 55629），agnes `.mcp.json` 握手 221 工具。
- 未 commit：`commercial/duduclaw-pro-gateway`（wrapper 分派＋Cargo 依賴）也要一起 commit（巢狀 repo）。

## 09-23 第七波：合併卡「外部系統與資料來源」＋四步精靈＋`odoo.*` 別名
- 使用者指出兩卡混淆、內建三列被誤認為已設定、設定太複雜 → 拍板 A＋精靈；畫布 9、10 通過。
- 落地：`RedactionSystemsCard.tsx`（Odoo／DB 連線／自訂來源／地端檔案固定列，讀進來／寫回去雙欄即時儲存，未歸屬寫回規則摺疊）、`RedactionSourceWizard.tsx`（類型→連線／工具→測試→寫回，Dialog，編輯從第 2 步）、`redactionSystems.ts`（egress key 推導、列組裝、規則計數，42 測試）；舊「外部系統」區塊、範本預設、`RedactionDataSourcesCard` 移除；規則表單來源下拉改顯示名稱。
- `odoo.*` egress key 從 v1.14 起對內建 Odoo 從未生效：`EgressEvaluator::new` 載入時別名為 `odoo_*`（顯式優先、不推廣）、UI 預設改 `odoo_*`、RFC-23／operator guide 範例修正、CHANGELOG Fixed。
- 驗收：web 1817／三語 5383 對齊；redaction 249＋18。本機重建 pro＋CE 並替換，gateway 重啟（pid 52197），新 bundle 上線，MCP 221 工具。
- 待拍板：精靈建的 DB 連線是否自動建立同名 `data_sources` 登錄項（讓每條連線各自有資料表挑選）。
- 09-23 12:03 精靈「來源名稱」欄位移除：識別碼由顯示名稱自動推導（`slugifyDataSourceName`：小寫、非 `[a-z0-9_]` 丟棄、純 CJK 退回 `source_<fnv6>`／`db_<fnv6>`、撞名加 `_2`、編輯凍結），只在進階設定唯讀顯示；文件四處同步。web 全量 1822、三語 5387。重建 pro＋CE 替換（備份 `.bak-20260923-120313`／`.bak-20260923-120308`），gateway pid 88968，bundle index-BPeZoaJt.js，MCP 221。

