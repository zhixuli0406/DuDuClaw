# Memory System Daily Smoke Test — 2026-05-16

**執行時間**：2026-05-16 03:07 UTC  
**執行者**：ENG-MEMORY (duduclaw-eng-memory)  
**規格依據**：specs/locomo-memory-eval-spec-v1.md §4.2  
**執行環境**：`duduclaw http-server` (127.0.0.1:8765) + API Key `ddc_dev_e6f5e54b3d33829bf97e54a989d32e26` (is_external=false, ns=internal/claude-desktop)

---

## 整體結果：✅ PASS（0 失敗，1 跳過）

| TC | 名稱 | 結果 | 耗時 |
|----|------|------|------|
| TC-1 | basic_store_and_retrieve | ✅ PASS | ~135ms (不含索引等待 500ms) |
| TC-2 | memory_isolation | ⏭️ SKIP | — |
| TC-3 | episodic_pressure_check | ✅ PASS | 45ms |

---

## TC-1：basic_store_and_retrieve

**目標**：寫入測試記憶後確認可被 unique token 精確召回。

- **Token**：`SMOKETEST_1778900839`
- **Content**：`SMOKETEST_1778900839 smoke_test_user 最愛的食物是拉麵`
- **Tags**：`["smoke-test", "temp", "locomo-eval"]`
- **Store 結果**：
  - memory_id：`9f2f5e9a-e2f7-4b28-88c9-59b5895a7d91`
  - namespace：`internal/claude-desktop`
  - stored_at：`2026-05-16T03:07:19.860934+00:00`
- **Search（exact token）**：找到 1 筆，content 完整匹配 ✅
- **Layer 分類**：`Episodic`，importance=5.0

**結論**：✅ TC-1 PASS — store OK, exact retrieve OK

---

## TC-2：memory_isolation

**目標**：確認跨 namespace 記憶不互相干擾。

**SKIP 原因**：`duduclaw http-server` MCP 層的 namespace hijacking prevention（mcp.rs §6380）會拒絕非 principal 允許 namespace 的請求（-32003 Namespace access denied）。本 API Key 的 principal 只允許 `internal/claude-desktop` 與 `shared/public`，無法傳入 `isolation_ns_a` / `isolation_ns_b` 等自訂 namespace。

**說明**：跨 namespace 隔離由 SQLite DB 層保障，unit test 覆蓋（crates/duduclaw-cli/tests/mcp_integration.rs TC-INT-23）。MCP 層本身是更強的隔離邊界（每個 API Key 僅能寫入一個 namespace）。

**後續行動**：W22 sprint 中考慮為 smoke test 配置 `smoke-test` 專屬 API Key，具備獨立 namespace（`internal/smoke-test-ns-a`、`internal/smoke-test-ns-b`）以真正驗證 DB 層隔離。

---

## TC-3：episodic_pressure_check

**目標**：呼叫 `memory_episodic_pressure` 確認回傳有效數值。

- **hours_ago**：24
- **raw_pressure**：`0.00`
- **normalized**：`0.000`（threshold=10.0）
- **Status**：✓ Below threshold
- **Response 文字**：
  ```
  Episodic pressure (last 24h): 0.00
  Threshold for Meso reflection: 10.0
  Status: ✓ Below threshold
  ```

**結論**：✅ TC-3 PASS — pressure=0.00 (raw), 0.000 (normalized), 為有效非負數值

---

## 技術備注

- **命名空間處理**：MCP HTTP server 統一注入 `internal/claude-desktop`，任何 `namespace` 參數被 namespace hijacking prevention 攔截，需省略。本次 TC-1、TC-3 均未傳 namespace。
- **TC-1 耗時說明**：完整 round-trip 635ms（含設計要求的 500ms index 等待），剔除等待後查詢耗時 ~135ms，符合 P95 < 200ms 目標。
- **smoke test API 路徑**：`POST /mcp/v1/call`（JSON-RPC 2.0），非 REST `/memory/store`。HttpMemoryClient 需配合 MCP 路徑；後續建議更新 `client.py` 以支援 MCP JSON-RPC transport。

---

*Generated by duduclaw-eng-memory (ENG-MEMORY) — 2026-05-16*
